# Subaru Reference

MkDocs site for my Subaru references/docs/whatever

```bash
# local setup
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
mkdocs serve --livereload
```
